import 'package:flutter/material.dart';

class FlashOfferScreen extends StatelessWidget {
  const FlashOfferScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Flash offer')),
    );
  }
}
